name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  unit:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install pytest
      - run: PYTHONPATH=src python -m pytest tests -q

  dogfood:
    # coretexa-verify checking its own pull requests
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      # No setup-python, no pip install: the Action installs this repo's own
      # declared test dependencies itself. That is the feature, dogfooded.
      - uses: ./
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
